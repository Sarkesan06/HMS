# Required Python packages for OAuth support
# Add these to your existing requirements.txt

# Google OAuth
google-auth>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.4.0
cachetools>=5.0.0

# Facebook OAuth
facebook-sdk>=3.0.0

# HTTP requests
requests>=2.28.0

# JWT (if not already installed)
PyJWT>=2.6.0
